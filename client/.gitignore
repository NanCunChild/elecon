# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/

# FVM
.fvm/

# gen_l10n 产物：由 `flutter pub get` / `flutter gen-l10n` 从 lib/l10n/*.arb 生成
# （权威源是 arb）。生成代码不入库，避免与 arb 双份维护 + diff churn。
lib/l10n/gen/
# gen_l10n 的缺译报告（arb 齐全时为空对象），本机产物
l10n_untranslated.json

# 原生 FFI 测试库：由 tool/build_qjs_test_lib.sh 本地构建，勿提交二进制
test/build/

# IDE
.idea/
*.iml

# Flutter 项目元数据：跨 SDK（官方主线 3.44.1 vs ohos fork 3.27.5-ohos）会被
# 各自重写、来回 churn，刻意不入库。flutter migrate 本项目未用。
.metadata
