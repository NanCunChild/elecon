## 现有 Schema 字段缺口

### `notice.list`

- `updatedAt`、置顶/重要程度和有效期。
- `author`、发布部门、受众范围和标签。
- 正文摘要、正文内容、附件列表及附件名称/地址。
- 通知状态：草稿、已发布、撤回、过期。
- 列表分页游标、总数、是否还有下一页。
- 分类应允许学校原始分类和标准分类同时保留，避免强行丢弃校内分类。

### `grades.list`

- 学年、学期原始标识与可读名称。
- 原始成绩、百分制成绩、等级成绩、绩点和成绩状态的分别表达。
- 重修、补考、缓考、免修、改分、退课等成绩来源/状态。
- 课程性质、课程类别、课程组、学分类型、教学语言。
- 任课教师、开课单位、班号、课序号、考试方式和考试时间。
- 排名、等级分布、课程平均分等可选统计信息。
- 成绩的 `unknown`、缺考、未出分等非数值状态，不能统一转成 0。

### `schedule.week`

- 学期起止日期、教学周起止日期和周次类型。
- 具体上课日期、节次时间表和跨日课程。
- 校区、楼栋、教室的结构化字段，不能只保留一个 location 字符串。
- 合班/分组、课程性质、教学班、授课语言和课程链接。
- 调课、停课、补课、代课、临时教室和变更原因。
- 线上课程地址、会议号、密码是否存在但不暴露敏感值。
- 同一课程多段时间、单双周、自定义周次和周次例外。
- 课表数据更新时间和来源系统。

### `card.balance` / `card.transactions`

- 卡片类型、账户类型、校区/钱包和卡片状态。
- 余额更新时间、交易唯一 ID、原始交易类型和交易地点。
- 交易发生时间与入账时间的区分。
- 退款、冲正、冻结、转账、补助等非普通消费方向。
- 分页游标、总数、余额快照时间和查询窗口。
- 失败、处理中、脱敏卡号等状态的统一表示。

### `library.loans`

- 借阅馆藏、索书号、馆藏位置和分馆。
- 续借次数、最大续借次数、可续借状态和逾期费用。
- 预约、取书截止时间、续借截止时间和归还确认状态。
- 借阅列表分页和历史借阅查询参数。

### `generic.section`

- 稳定的 section 类型/版本，而不是只使用自由文本 `sectionId`。
- 可选的来源系统、抓取时间、有效期和刷新建议。
- 分页、局部失败和字段级脱敏标记。
- 表格列的排序、单位、链接和金额/时间语义。
- 原始字段别名或扩展字段的命名空间，避免各校互相冲突。

## 需要新增的标准能力

优先级从高到低：

### P0：大多数学校都需要

- `profile.me`：姓名、学号脱敏标识、院系、专业、年级、身份类型、当前校区。
- `term.list` / `calendar.academic`：学年学期、起止日期、教学周、节假日和当前学期。
- `exam.list`：考试时间、课程、地点、座位、考试类型、状态和变更信息。
- `classroom.available`：日期、时间段、楼栋、教室容量、设备和占用状态。
- `attendance.summary`：考勤汇总、缺勤类型、课程、日期和申诉状态。

### P1：多个学校已有真实接口

- `library.seats`：馆区、楼层、座位、可用状态、预约窗口。
- `library.booking`：预约记录、房间/座位、开始结束时间、取消和签到状态。
- `energy.usage`：水、电、宿舍/房间、读数、用量、时间范围和单位。
- `course.catalog`：课程检索、课程详情、教师、容量、先修课和开课学期。
- `course.selection`：可选课程、已选课程、退选/冲突/容量状态。
- `gpa.summary`：总绩点、学分统计、排名和统计窗口。
- `notice.detail`：通知正文、附件和更新时间。

### P2：长尾但有明确复用价值

- `dorm.health`：宿舍健康/体测结果、指标、时间和状态。
- `dorm.service`：宿舍报修、门禁、入住/退宿和服务状态。
- `transport.schedule`：班车线路、站点、班次、运营日期和实时状态。
- `dining.summary`：食堂/商户消费汇总和营业状态；交易明细仍复用校园卡能力。
- `invoice.list`：发票号码、金额、开票时间、状态和下载引用。
- `program.progress`：培养方案要求、已完成学分、缺口和完成状态。
- `research.income`：研究生助学/科研收入的月份、金额、状态和来源。
- `campus.network`：设备列表、网络账号状态、流量/在线状态；不能暴露网络凭证。
- `app.announcement`：客户端版本公告、隐私文档和非用户数据的应用元信息。

## 跨学校契约需要统一的机制

- 统一分页：`page`/`size` 与 cursor 二选一的标准表达、总数和下一页标记。
- 统一时间：所有日期时间使用 RFC3339/UTC，同时保留学校本地时区或原始日期语义。
- 统一金额：整数最小货币单位、ISO 币种、正负方向和未知金额。
- 统一状态：`unknown`、`pending`、`final`、`cancelled`、`expired`、`failed` 等状态的适用边界。
- 统一空值：区分“不支持”“接口未返回”“确实为空”和“脱敏后不可见”。
- 统一来源：来源系统、请求时间、数据更新时间和可选的源记录 ID。
- 统一错误：认证失效、验证码/人工交互、权限不足、接口变更、局部字段失败和暂时不可用。
- 统一增量：课表变更、成绩更新、通知撤回、余额变化的更新时间或版本字段。
- 统一原始扩展：允许学校 adapter 在命名空间下提供扩展，但不得改变标准字段语义。
- 统一脱敏：学生身份、卡号、二维码、票据、Cookie、Token、带凭证 URL 和附件中的个人信息不能进入 schema golden 或公开 fixture。

## 主仓库需要完成的行动

- [ ] 为 P0/P1 能力确定正式 capability ID、输入参数和输出 schema。
- [ ] 为每个新能力建立注册表条目、JSON Schema、TypeScript 产物和 Dart 产物。
- [ ] 明确必填/可选字段、空值语义、枚举范围和向后兼容策略。
- [ ] 为成绩、课表、考试、空教室、考勤、校园卡、图书馆和能源提供跨校通用字段基线。
- [ ] 将学校原始字段与标准字段的映射规则固定下来，允许 adapter 保留受控扩展。
- [ ] 增加分页、时间、金额、状态、来源、错误和增量更新的公共定义。
- [ ] 为 WebVPN、多跳统一认证、验证码、人工登录和会话过期定义宿主侧能力；adapter 不应自行保存或转发凭证。
- [ ] 提供脱敏响应回放和 schema golden 测试入口，使每所学校可以在不访问真实账号的情况下验证 adapter。
- [ ] 更新 validator，使新增 capability、参数、版本和扩展字段能够被静态校验。
- [ ] 更新 catalog/分发校验，使新能力不会被 catalog 静默丢弃。
- [ ] 形成契约版本升级和兼容性检查，避免 vendor 同步后 adapter 被突然拒载。
- [ ] 发布新的契约快照后，同步更新本仓库 `vendor/`；同步应是独立生成提交，不混入学校 adapter 改动。

