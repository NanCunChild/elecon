有人提出异议，认为服务器端使用goja并不合适，部分语法不支持，并且意味着一定要将版本钉死在es6左右作为安全基线和语法模板。请问是这样吗？

两个都是好问题，而且第二个比它看起来更根本。分开说。

## 1. 服务端改用 Node.js

倾向支持，而且上一轮那个 goja 难题恰好让 Node 的性价比更高了——但有三个必须配套的前提，否则会埋雷。

先说为什么这次比当初选 Go 时更站得住：

**上一轮的 goja 缺口问题，本质是 Go 专属的税。** 它来自"服务端被迫用一个不完整的纯 Go 引擎"。换 Node 后，服务端引擎是 V8——一个比客户端 QuickJS 更完整的现代引擎，**它不再是那个受限的一方**，"必须钉 ES6 基线"的压力直接消失。

**原来选 Go 的"性能优先"理由，对这个负载其实偏弱。** 你的服务端是反代 + 缓存 + 发 adapter，是 **I/O 密集**，而 I/O 并发恰好是 Node 事件循环的主场。Go 的 goroutine 优势在 CPU 密集场景才明显；真有重解析，Node 用 worker_threads 卸载即可。所以"为性能上 Go"在这里没占到多少便宜。

**真正的大头是你说的"极大兼容性"，这点你说对了。** 全栈统一到 JS/TS：adapter 是 JS、服务端是 JS、契约校验工具(ajv 之类)客户端服务端共用一套、团队最熟的语言、一个心智模型。对一个"人力不足"的项目，**统一语言带来的维护收益，远大于逐个组件抠性能**。这正合 ADR-000 的低维护主线。

三个前提（缺一不可）：

- **必须上 TypeScript，不是裸 Node。** broker、凭证、签名这些承重路径没有静态类型迟早出事。"Node"在这个项目里应读作"TypeScript on Node"。
- **adapter 绝不能用 Node 的 `vm` 模块跑。** Node 的 `vm` **明确不是安全边界**，半可信/侧载 adapter 在里面等于裸奔。这里有个比 quickjs-go 更优雅的解法:**在 Node 里用编译成 wasm 的 QuickJS（如 quickjs-emscripten 这类）跑 adapter**——你同时拿到三样东西:真正的沙箱、**与客户端字面意义上同一个引擎(零语义漂移)**、且全程纯 JS/wasm 无 cgo。这比 Go+goja(有缺口)和 Go+quickjs-go(要 cgo)都干净。isolated-vm 是另一条路,但去用前先查它当前的维护状态。
- **部署与供应链要补课。** Go 白送的单静态二进制，Node 这边要用 Node 单可执行打包(SEA)或 Docker 补上;node_modules 的供应链风险对**校内那台经手凭证的服务器**尤其要紧——锁 lockfile、最小依赖、定期 audit。公网那台是无状态哑服务,风险低,随意些没关系。

一句话：**Node/TS + QuickJS-wasm 跑 adapter** 是我会推荐的组合，它把上一轮的引擎漂移问题和这一轮的维护诉求一起解了。这正是 `adr_005`（引擎/运行时选型）该拍板的内容——把决策从"Go vs Node"重述成"宿主语言 + adapter 执行沙箱"。

## 2. 软件本体要不要"向上适配课表/成绩"字段

直接回答：**要保留 typed 模型（本体确实"认识"成绩/课表），不要把样式和变量选择交给 adapter。** 你描述的"全部抽象为样式命名、最终变量和样式由 adapter 提供"——这个方向**转过头了**，会同时打掉四样东西：

- **本体的智能。** 一旦本体不知道"这是成绩"，它就**算不了 GPA、排不了序、做不了语义搜索、跨校聚合不了**；课表不知道是课表，就做不了"上课前 10 分钟提醒"。所有需要理解数据的能力全废。一个只会渲染、不理解数据的本体，**本质上是个更差的浏览器**——而你做聚合器的全部意义，正是做浏览器做不到的事(统一、计算、提醒、搜索)。typed 模型不是官僚开销，**它是产品价值的来源**。
- **一致性。** adapter 各自选样式 = 20 个学校 20 套视觉语言，拼贴感，而且把设计负担甩给了本就不是设计师的学生作者。统一体验是聚合器的卖点，不能让 adapter 拆了。
- **安全模型。** 你前面辛苦建的信任分层(侧载 adapter 退化为纯解析器)，就是为了**限制 adapter 能做什么**。让 adapter 驱动 UI/样式,等于把半可信代码放进渲染层——能伪造误导性界面、仿冒"官方"横幅,blast radius 又炸回去了。
- **服务端缓存。** 公网缓存、双跑一致性测试，都建立在"数据被归一化成已知 schema"之上。全是 adapter 自定义的不透明 blob,缓存什么也做不了。

**但你提这个,真正想解的是"新增/冷门数据类型就得走 ADR 慢车道"的摩擦——这个痛点是真的,有更好的解法,不必走极端:**

加**一个**通用的、**由本体渲染**的兜底类型，比如 `elecon.generic.section`——受约束的"带标签字段 / 键值组 / 简单表格"。长尾的、不值得单独立 schema 的校园特有数据（宿舍水电、实验室预约之类）走这个类型，**不必每个字段都开 ADR**。关键在：**本体仍然拥有渲染权和样式**，adapter 只提供归一化数据 + 轻量语义提示，绝不提供 CSS/控件/样式名。

这里要分清两个东西，差别就是成败：

- **语义角色提示（adapter 可以给）**：标注"这个字段是状态 / 是截止时间 / 是金额 / 是主标识 / 是链接"。
- **样式（adapter 绝不能给）**：具体颜色、控件选择、布局代码。

adapter 说"这是个 deadline"，**本体的主题**决定 deadline 长什么样。这样长尾灵活性有了，一致性、安全、本体智能一个没丢。"样式命名"这个想法里有价值的内核(语义化),被保留进了"角色提示";有害的部分(adapter 决定外观)被挡在门外。

落点很清楚：这是契约决策，属 ADR-001——给 schema 域列表**加一个 `generic` 域**(数据 + 角色枚举);"角色如何映射到主题样式"属 `adr_004`(SDUI/UI 层)。

需要的话，我可以把这两条决定回写进 ADR：`adr_005` 落"Node/TS + QuickJS-wasm"，ADR-001 补 `generic` 域与语义角色枚举。