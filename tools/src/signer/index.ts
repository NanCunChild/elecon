/**
 * signer：官方 adapter 签名 / 吊销。
 *
 * 仅官方签名 adapter 可用 fetch 模式；传输底座仅官方签名可加载（红线 #4）。
 * 签名相关是安全敏感路径——AI 不得独自闭环，需人工审阅（AGENTS.md §1）。
 *
 * 尚未实现。
 */

function main(): void {
  console.log("adapter signer (Node/TS): not implemented yet");
}

main();
